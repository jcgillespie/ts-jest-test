{
  "name": "ts-jest-test",
  "version": "1.0.0",
  "description": "repro repo for unhandled rejection",
  "main": "index.js",
  "author": "Josh Gillespie",
  "license": "ISC",
  "scripts": {
    "test:works": "jest --coverage --no-cache --config ./jest.working.json",
    "test:errors": "jest --coverage --no-cache --config ./jest.errors.json"
  },
  "dependencies": {
    "jest": "^19.0.2",
    "ts-jest": "^19.0.9",
    "typescript": "^2.2.2"
  }
}